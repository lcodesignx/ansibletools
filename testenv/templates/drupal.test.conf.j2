<virtualhost *:80>
    ServerAdmin drupal@localhost
    ServerName {{ domain }}
    ServerAlias www.{{ domain }}.lco.net
    DocumentRoot {{ drupal_core_path }}
    <Directory "{{ drupal_core_path }}">
        Options FollowSymLinks Indexes
        AllowOverride All
    </Directory>
</virtualhost>